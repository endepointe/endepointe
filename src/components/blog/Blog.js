/**
 * When this component loads, all of my blog posts will appear.
 *  
 * Each blog post will serve as the root for the ResponsePost child
 * components.
 * 
 * When this component loads, each BlogPost component will fetch the list of 
 * ResponsePosts using its unique id.
 */
export default function Blog() {
	return (
		<div>
			
		</div>
	)
}